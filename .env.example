# Database
DATABASE_URL="postgresql://admin:pourtoutfaireproprement@localhost:5432/sig_dev?schema=public"

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=super_secret_local_2024

// configuration du transporteur pour MailHog
const transporter = nodemailer.createTransport({
  host: 'mailhog', // ou 'mailhog' si dans docker-compose
  port: 1025,
  secure: false, // MailHog n'utilise pas SSL/TLS par défaut
  auth: {
    user: '', // Pas besoin d'authentification pour MailHog
    pass: '',
  },
});

#Mollie

MOLLIE_API_KEY=test_pErmE26udFd7cbKzjMyttNHABjdqcb
Profile_Id=pfl_odM84rFXuP

# URL publique de votre application (doit être accessible par Mollie pour le webhook)
# En développement local, utilisez ngrok : https://ngrok.com/
#   ex: ngrok http 3000  →  APP_BASE_URL=https://abc123.ngrok.io
APP_BASE_URL=https://votre-domaine.com

# URL du front-end (pour les headers CORS)
FRONTEND_URL=http://localhost:3001

# Port d'écoute du serveur Express
PORT=3000